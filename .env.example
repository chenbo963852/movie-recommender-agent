# LLM 后端配置
# backend: cloud (OpenAI 兼容 API) 或 local (本地 transformers 模型)
LLM_BACKEND=cloud

# Cloud 后端配置
LLM_API_KEY=your_api_key_here
LLM_BASE_URL=https://api.openai.com/v1
LLM_MODEL=gpt-4o-mini

# 本地模型路径（仅 LLM_BACKEND=local 时生效）
# LLM_LOCAL_MODEL_PATH=local_models/Qwen2.5-0.5B-Instruct
